import Upload from "../../components/Upload";

export default function fileJson(){
  return(
    <Upload files={undefined} setFiles={undefined} hiddenPreviewPhoto={undefined} viewIconPdf={undefined} typeUpload={"application/json"} textError={undefined} />
  )
}